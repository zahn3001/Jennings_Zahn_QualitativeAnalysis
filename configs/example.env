# Copy this file to .env and update with your own values.
# Load with: export $(cat .env | xargs)

# OpenAI API key (required)
OPENAI_API_KEY="sk-your_api_key_here"

# Default model (optional; can override in CLI)
OPENAI_MODEL="gpt-4o"
